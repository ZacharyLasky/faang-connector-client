import React from 'react';
import styled from 'styled-components';
import { colors } from '../../../../styles';

export const AllCandidates = ({ candidates, job, setSelectedCandidate }) => {
  return <>hi</>;
};
